*conf*.json
.DS_Store
__pycache__
.idea/
.env
# Local deploy config (copied from config.template.yaml) -- never commit.
config.yaml
# Local virtualenvs
.venv/
# OMC / agent session + state -- never commit to the shared repo
.omc/
.isaac/
# Python bytecode + test caches
*.pyc
.pytest_cache/

